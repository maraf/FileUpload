@model IndexViewModel
@{
    ViewData["Title"] = "Upload";
}

<partial name="_UploadForm" />

@if (Model.Browser != null)
{
    <partial name="_Browser" model="@Model.Browser" />
}

<div class="version">@Model.AppVersion</div>

@section Scripts {
<script>

    var Url = '@Model.Upload.Url';
    Initialize();

</script>
}